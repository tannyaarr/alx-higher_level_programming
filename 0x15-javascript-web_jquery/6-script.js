// script that updates the text of the <header> to a New Header
$('#update_header').on('click', function () {
  // Update the text of the header to New Header!!
  $('header h1').text('New Header!!!');
});
